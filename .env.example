# Hyperliquid API 配置（敏感信息）
HYPERLIQUID_WALLET_ADDRESS=your_wallet_address_here
HYPERLIQUID_PRIVATE_KEY=your_private_key_here

# 环境选择（固定 mainnet）
ENVIRONMENT=mainnet

# 初始净值（USD）
INITIAL_NAV=100000

# 风控参数（可选，会覆盖 YAML 配置）
MAX_SINGLE_LOSS_PCT=0.008  # 单笔最大亏损（0.8% 净值）
MAX_DAILY_DRAWDOWN_PCT=0.05  # 日最大回撤（5%）

# 信号阈值（可选，会覆盖 YAML 配置）
THETA_1=0.5  # 高置信度阈值
THETA_2=0.2  # 低置信度阈值

# 日志配置
LOG_LEVEL=INFO              # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_DIR=logs                # 日志文件目录
LOG_RETENTION_DAYS=30       # 日志保留天数（自动轮转）
ENABLE_AUDIT_LOG=true       # 是否启用审计日志（记录关键操作）

# ============================================================
# 其他配置说明
# ============================================================
#
# 以下配置应该在 YAML 文件中设置，而不是环境变量：
#
# - obi_levels: 在 config/week1_ioc.yaml 中配置
# - max_position_size: 在 config/base.yaml 的 risk.hard_limits 中配置
# - log_level: 使用 --log-level 命令行参数
# - metrics_port, alert_webhook: 在 YAML 中的 monitoring 部分配置
#
# 环境变量应该只用于：
# 1. 敏感信息（钱包地址、私钥）
# 2. 需要动态覆盖 YAML 配置的核心参数
